# Z420 ADB Host: Proxmox & USB Passthrough Guide\n\nTo control your physical devices (S24 Ultra, A40, Moto G40, P10) from the MeLe Brain, we need to configure the **HP Z420** as a dedicated ADB (Android Debug Bridge) Host.\n\n## 🏗️ The Setup\n- **Physical:** Connect the **Powered USB Hub** to a USB 3.0 port on the HP Z420.\n- **Virtual:** Create a dedicated LXC container (`traffic-grid-adb`) on Proxmox.\n- **Passthrough:** Map the entire USB Hub (or individual modems/phones) to this LXC.\n\n## 🛠️ Step 1: Identify USB Devices (Proxmox Host)\nRun this on the Z420 shell:\n```bash\nlsusb\n```\nLook for the ID of your Hub and Phones (e.g., `ID 04e8:6860 Samsung Electronics Co., Ltd Galaxy series`).\n\n## 🛠️ Step 2: Configure LXC Passthrough\nEdit the LXC configuration file on the Proxmox host (`/etc/pve/lxc/XXX.conf`):\n```bash\n# Allow access to USB devices\nlxc.cgroup2.devices.allow: c 189:* rwm\nlxc.mount.entry: /dev/bus/usb dev/bus/usb none bind,optional,create=dir\n```\n\n## 🛠️ Step 3: Start the ADB Server (Inside LXC)\n1. Install ADB:\n   ```bash\n   apt update && apt install -y adb\n   ```\n2. Start the server in **Network Mode**:\n   ```bash\n   adb kill-server\n   adb -a nodaemon server start &\n   ```\n   *Note: The `-a` flag allows the MeLe to connect to this ADB server over the network.*\n\n## 🛠️ Step 4: Connect from the MeLe (The Brain)\nOn the MeLe, the `mobile_controller.py` will now connect to the Z420's IP:\n```python\n# In mobile_controller.py\nself.d = u2.connect(\